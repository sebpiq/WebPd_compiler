import { MSG_TOKEN_TYPE_FLOAT, MSG_TOKEN_TYPE_STRING } from '../constants'

// Assemblyscript representation of message token types
export const MSG_TOKEN_TYPES_ASSEMBLYSCRIPT = {
    [MSG_TOKEN_TYPE_FLOAT]: 0,
    [MSG_TOKEN_TYPE_STRING]: 1,
}
